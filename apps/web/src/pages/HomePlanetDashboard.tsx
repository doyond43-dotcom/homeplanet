/**
 * TEMP STUB — Intake ship unblock
 * The prior file was corrupted ("void USAGE_COUNTS" etc).
 * Replace with real dashboard after build is green.
 */
export default function HomePlanetDashboard() {
  return (
    <div style={{ padding: 24 }}>
      <h1 style={{ margin: 0, fontSize: 22 }}>HomePlanet Dashboard</h1>
      <p style={{ opacity: 0.8, marginTop: 10 }}>
        Dashboard temporarily disabled while we ship Intake. Build is unblocked.
      </p>
    </div>
  );
}
