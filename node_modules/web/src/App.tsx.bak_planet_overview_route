import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
import { AppShell } from "./layout/AppShell";
import CoreHome from "./pages/CoreHome";
import PlanetOverview from "./pages/PlanetOverview";
import CityPage from "./pages/CityPage";
import CreatorProjects from "./pages/CreatorProjects";
import CreatorBuild from "./pages/CreatorBuild";
import SimpleDocPage from "./pages/SimpleDocPage";

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route element={<AppShell />}>
          {/* Core */}
          <Route path="/" element={<CoreHome />} />
          <Route
            path="/core/doctrine"
            element={<SimpleDocPage title="Doctrine" body="HomePlanet doctrine lives here. Next: render the canonical doctrine + excerpts." />}
          />
          <Route
            path="/core/binder"
            element={<SimpleDocPage title="Binder Map" body="HomePlanet binder map lives here. Next: show volumes + quick links." />}
          />

          {/* Planets */}
          <Route path="/planet/:planetId" element={<PlanetOverview />} />
          <Route path="/planet/creator/projects" element={<CreatorProjects />} />
<Route path="/planet/creator/build" element={<CreatorBuild />} />
<Route path="/planet/:planetId/:cityId" element={<CityPage />} />

          {/* Default */}
          <Route path="*" element={<Navigate to="/" replace />} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}



